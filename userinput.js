//Try creating a program that asks for user input and performs a calculation.

let number = prompt("Enter a number")
let square = number * number 
console.log("The square of a number:" + number +"is" + square);